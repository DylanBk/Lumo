import { SessionPayload } from "@/lib/definitions";

type Props = {
  userData: SessionPayload | null;
};

const Home = (props: Props) => {
  return (
    <main>

    </main>
  );
};

export default Home;